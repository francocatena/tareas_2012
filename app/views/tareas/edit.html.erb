<div class="page-header">
  <h1><%= t('.titulo') %></h1>
  <nav>
    <%= link_to t('etiqueta.ver'), @tarea %> |
    <%= link_to t('etiqueta.volver'), tareas_path %>
  </nav>
</div>

<%= render 'form' %>
