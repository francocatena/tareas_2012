<%= simple_form_for(@responsable) do |f| %>
  <%= f.error_notification %>

  <%= f.input :nombre %>
  <%= f.input :email %>
  <%= f.input :clave, as: :password %>
  
  <div class="actions">
    <%= f.button :submit, class: 'btn-primary' %>
  </div>
<% end %>
